# sonic-tuna
Sonic Tuna Project - проект виртуального тоннеля поверх онлайн конференций.

## Принцип работы

Данный проект позволяет вам установить канал передачи данных через изображение веб камеры, передаваемое через онлайн конференцию.
Когда вы запускаете скрипт tuna-server.py или фоновый сервис sonic-tuna.service, он запускает headless версию браузера chromium, подключается к звонку и использует виртуальную веб камеру, чтобы передавать данные. Данные с вебкамер собеседников преобразуются обратно в данные.

Ниже приведена общая схема Sonic-Tuna
![Image](https://github.com/user-attachments/assets/924d2387-f4ca-4b7d-8662-b3f106e0cd4c)

Для кодирования данных в изображение используются частотные модуляции (0 -> -40, 1 -> +40). Формируются матрицы 8x8 и заполняются 16 бит информации (левый верхний угол матрицы, по принципу змейки). Затем через обратное косинусное преобразование из матрицы частот получается матрица яркости блока пикселей 8x8. Картинка 256x256 вмещает в себя 1024 блока, каждый из которых хранит 2 байта информации. Итого через изображение 256x256 передается порядка 2048 байт информации (часть из которой - метаданные и коды CRC32/ReedSolomon).

Декодирование происходит в обратном порядке - к каждому блоку 8x8 применяется прямое дискретное косинусное преобразование, знаки частотных коэфециентов интерпретируюся как биты, из которых формируются слова по 16 бит.

Более подробное объяснение в мое [тгк](https://t.me/jawa_prog_channel) :)

## Установка

Для установки Sonic Tuna как systemd сервиса используйте скрипт `install.py`. Скрипт требует прав администратора (root).

### Базовая установка (рекомендуется)

```bash
sudo apt update
sudo apt install python3 python3-venv
sudo python3 install.py
```

Будут использованы параметры по-умолчанию:

- Пользователь: `sonic-tuna`
- IP адрес: `10.0.0.1`
- URL конференции: (не указан)

### Установка с параметрами

```bash
# Обновить установленные файлы
sudo python3 install.py --update

# Обновить для пользовательского пользователя
sudo python3 install.py --update --user myuser

# Указать пользовательский IP адрес и URL конференции
sudo python3 install.py --ip 10.0.0.1 --url "https://telemost.yandex.ru/j/12345678"

# Указать пользовательского пользователя
sudo python3 install.py --user myuser --ip 10.0.0.1

# Все параметры
sudo python3 install.py -u myuser --ip 10.0.0.1 --url "https://telemost.yandex.ru/j/12345678"
```

### Доступные параметры install.py

- `--update` — обновить установленные файлы (без пересоздания конфигурации и сервиса)
- `-u, --user` — имя системного пользователя (по-умолчанию: `sonic-tuna`)
- `--ip` — IP адрес сетевого интерфейса (по-умолчанию: `10.0.0.1`)
- `--url` — ссылка на конференцию Яндекс Телемост (по-умолчанию: пусто)

### Что делает установщик

1. Создаёт системного пользователя `sonic-tuna`
2. Копирует файлы проекта в `/home/sonic-tuna/`
3. Создаёт виртуальное окружение Python и устанавливает зависимости
4. Устанавливает Chromium для Playwright
5. Создаёт конфигурационный файл `/etc/sonic-tuna/config.yaml`
6. Создаёт systemd сервис `/etc/systemd/system/sonic-tuna.service`

### Завершение установки

После завершения установки необходимо отредактировать конфигурационный файл (он содержит описание всех доступных параметров):

```bash
sudo nano /etc/sonic-tuna/config.yaml
```

Затем запустить сервис:

```bash
sudo systemctl start sonic-tuna
sudo systemctl enable sonic-tuna  # Для автозапуска при загрузке системы
```

## Удаление

Для удаления Sonic Tuna из системы используйте скрипт `uninstall.py`. Скрипт требует прав администратора (root).

### Удаление

```bash
sudo python3 uninstall.py
```

Будет использован пользователь по-умолчанию `sonic-tuna`.

### Удаление с другим пользователем

```bash
sudo python3 uninstall.py --user myuser
```

или

```bash
sudo python3 uninstall.py -u myuser
```

### Что делает скрипт uninstall.py

1. Останавливает systemd сервис `sonic-tuna`
2. Удаляет системного пользователя и его домашнюю директорию
3. Удаляет конфигурационный файл `/etc/sonic-tuna/`
4. Удаляет systemd сервис `/etc/systemd/system/sonic-tuna.service`
5. Перезагружает конфигурацию systemd

**Внимание:** Удаление необратимо и удалит все файлы пользователя, указанного через флаг `--user`.  
**Команда разработки не несет ответственности за ущерб, который может принести uninstall.py в случае указания не того
пользователя**
